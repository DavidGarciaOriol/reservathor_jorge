<div class="d-flex justify-content-center">
        {{ $rooms->links() }}
    </div>

    @include('public.rooms.partials.partialPaginate')

    <div class="d-flex justify-content-center">
        {{ $rooms->links() }}
    </div>